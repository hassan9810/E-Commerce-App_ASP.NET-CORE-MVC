@model Producer

@{
    ViewData["Title"] = "Update Existing Producer ";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h2 class="text-center text-success">
                Update Existing Producer => @Model.FullName
            </h2>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group text-center">
                        <img class="border-primary rounded-circle" style="max-width:150px" id="PicturePreview" />
                    </div>
                    <div class="form-group">
                        <label asp-for="ProfilePictureURL" class="control-label mb-1"></label>
                        <input asp-for="ProfilePictureURL" class="form-control mb-2" />
                        <span asp-validation-for="ProfilePictureURL" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="FullName" class="control-label mb-1"></label>
                        <input asp-for="FullName" class="form-control mb-2" />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Bio" class="control-label mb-1"></label>
                        <input asp-for="Bio" class="form-control mb-3" />
                        <span asp-validation-for="Bio" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Update" class="btn btn-outline-success float-end" />
                        <a class="btn btn-outline-secondary" asp-action="Index">Show All Actors</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script>
        $(document).ready(function () {
            var output = document.getElementById('PicturePreview');
            output.src = $(ProfilePictureURL).val();
        });
        $("#ProfilePictureURL").on("change", function () {
            var output = document.getElementById('PicturePreview');
            output.src = $(this).val();
        })
    </script>
}