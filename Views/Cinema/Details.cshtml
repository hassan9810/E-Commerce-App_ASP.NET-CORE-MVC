@model Cinema

@{
    ViewData["Title"] = "Cinema Details";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h2 class="text-center text-success">
                Details for Cinema @Model.Name
            </h2>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                    <div class="form-group text-center">
                        <img class="border-info rounded-circle" style="max-width:150px" id="PicturePreview" />
                    </div>
                    <div class="form-group">
                        <label asp-for="Logo" class="control-label mb-1"></label>
                    <input asp-for="Logo" class="form-control mb-2" readonly />
                    <span asp-validation-for="Logo" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Name" class="control-label mb-1"></label>
                    <input asp-for="Name" class="form-control mb-2" readonly />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Description" class="control-label mb-1"></label>
                    <input asp-for="Description" class="form-control mb-3" readonly />
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                    @if (User?.Identity?.IsAuthenticated == true && User.IsInRole("Admin")) 
                    {
                        <a class="btn btn-outline-success float-end" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
                    }
                        <a class="btn btn-outline-secondary" asp-action="Index">Show All Cinemas</a>
                    </div>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script>
        $(document).ready(function () {
            var output = document.getElementById('PicturePreview');
            output.src = $(Logo).val();
        });
    </script>
}